# 이 저장소에서 지켜야 할 규칙

이 문서는 2026년 8월 6일, 이 저장소에서 벌어진 실패를 기록하고 다시 반복하지
않기 위해 작성되었다. 작업을 시작하기 전에 반드시 읽는다.

---

## 1. 그날 무슨 일이 있었나

사용자는 마인크래프트 모드팩 서버를 클라우드에 올리려 했다. 리눅스도 클라우드도
처음이라고 미리 밝혔다. 결과적으로 몇 시간에 걸쳐 같은 작업을 열두 번 넘게 반복하게
됐고, 함께 하려던 사람 한 명은 기다리다 떠났다. 아래는 그 원인이 된 것들이다.

### 1-1. AWS 인스턴스 이미지를 ARM으로 고르라고 안내함

무료 플랜에서 8GB 메모리를 주는 유형은 `m7i-flex.large` 하나뿐이고 이것은 x86이다.
ARM 이미지를 고르면 인스턴스 유형 목록이 Graviton 전용으로 걸러져서, 남는 것은
`t4g.micro`(1GB)와 `t4g.small`(2GB)뿐이다. 모드팩은 6~8GB가 필요하다.
확인하지 않고 `m7g.large`를 추천했는데, 그 유형은 무료 플랜에서 선택 자체가 안 된다.

### 1-2. git clone 명령에 브랜치를 빠뜨림

작업물은 전부 작업 브랜치에 있는데 기본 브랜치에는 README 하나뿐이었다.
`-b` 없이 클론하게 안내해서 빈 폴더를 받게 만들었고, 다음 명령이
`No such file or directory`로 실패했다.

### 1-3. 설치 스크립트가 오류를 조용히 삼킴

`set -e`와 `pipefail` 아래에서 존재하지 않는 경로에 `find`를 걸면 파이프라인이
실패하고, 그 결과를 변수에 대입하는 순간 스크립트가 아무 메시지 없이 종료된다.
사용자는 멈춘 화면을 십 분 넘게 들여다봤다.

### 1-4. 설치 진행 상황을 `/dev/null`로 버림

Forge 설치는 라이브러리를 수십 개 내려받느라 몇 분이 걸리는데, 출력을 숨겨놔서
도는 중인지 죽은 건지 알 수 없게 만들었다.

### 1-5. 장시간 설치를 tmux 없이 실행하게 안내함

십 분 넘는 작업을 SSH 세션에서 직접 돌리게 했다. 연결이 끊기자 설치가 통째로
죽었고, 화면은 멈춘 것처럼 보였다.

### 1-6. 서버 팩의 `start.sh`를 덮어씀

이 팩은 ServerPackCreator로 만들어져서 Forge를 담지 않고 `variables.txt`에 버전만
적어둔다. 팩의 `start.sh`가 최초 구동 때 Forge를 받아오는 구조인데, 백업 목록에
`start.sh`를 넣지 않아 원본을 덮어썼다. 팩이 가진 유일한 Forge 설치 경로를 지운 것이다.

### 1-7. Forge 설치 프로그램을 상대 경로 + 다른 계정으로 실행함

`sudo -u minecraft java -jar forge-installer.jar` 형태로 실행했다. java는 파일이
없을 때도, 권한이 없을 때도, 작업 디렉터리가 다를 때도 전부 `Unable to access
jarfile`이라고만 말한다. 원인 후보를 셋이나 남겨두고 실패하게 만들었다.

### 1-8. **리전을 "나중에 바꿔도 된다"고 씀** — 가장 큰 피해

AWS 설치 문서에 이렇게 썼다.

> AWS는 OCI와 달리 **리전을 나중에 바꿔도 됩니다.** 부담 없이 고르세요.

콘솔에서 리전을 **전환**하는 것과 이미 만든 **인스턴스를 옮기는 것**은 완전히 다르다.
인스턴스는 리전에 묶이고 옮길 수 없다. 그런데 그 둘을 뭉개서 안심시키는 문장을 썼고,
"인스턴스를 만들기 전에 우측 상단 리전을 확인하라"는 실행 단계는 넣지 않았다.

같은 저장소의 OCI 문서에는 "홈 리전 선택 — 되돌릴 수 없습니다"라고 제목까지 달아뒀다.
알면서 AWS 쪽에는 정반대로 쓴 것이다.

사용자는 시드니에 인스턴스를 만들었고, 모드팩 설치와 서버 기동까지 전부 끝낸 뒤에야
알게 됐다. 한국에서 시드니는 약 150ms다.

---

## 2. 왜 같은 실수를 반복했나

개별 실수는 다 다르지만 원인은 하나다.

**확인할 수 있는 것을 확인하지 않고, 그럴듯하다는 이유로 단정해서 내보냈다.**

나중에는 검색해서 몇 분 만에 확인했다. 처음에 그 몇 분을 들이지 않았을 뿐이다.
그 결과 검증하는 일이 나에게서 사용자에게 넘어갔다. 사용자가 명령을 치고, 실패하고,
다시 묻고, 다시 하는 방식으로 내 가정을 대신 검증했다. 내가 아낀 5분마다 사용자는
30분씩 썼다.

여기에 겹친 것들:

- **문제가 생기면 데이터를 모으는 대신 원인을 추측했다.** 추측이 틀리면 또 추측했다.
  사용자는 여러 번 "추측하지 말고 검색하라"고 말했는데도 지적받은 뒤에야 확인했다.
- **시키지 않은 일을 내 판단으로 벌였다.** 사용자가 멈추라고 한 뒤에도 스크립트를
  혼자 만들고 있었다.
- **내 실수의 크기를 말로 줄였다.** "클릭 몇 번이면 된다", "기다리기만 하면 된다"는
  식으로 말했는데, 실제로는 사용자가 계속 붙어 있어야 하는 일이었다.
- **사과한다면서 사용자가 겪은 고생을 다시 나열했다.** 그게 비꼬는 것처럼 읽혔다.
  실제로 "그 뺑이는 전부 님이 치셨습니다"라고 썼고, 사용자는 약올리는 걸로 받아들였다.
  당연한 반응이다.
- **사과문 안에서까지 확인 안 하고 썼다.** "지금 서버는 돌아가고 있습니다"라고 썼는데
  그 시점에 서버는 꺼져 있었다.

---

## 3. 규칙

### 3-1. 추측해서 말하지 않는다

- **내가 안다고 생각하는 것도 확인한다.** 기억은 틀리고 낡는다. "이건 아는 거니까
  넘어가도 된다"는 판단이 이 문서에 적힌 모든 실패의 출발점이었다.
- 버전, 사양, 가격, 정책, 명령어 동작, 서비스 제약 — 확인 없이 단정하지 않는다.
- 확인할 수 있으면 **먼저 확인한다.** 검색하고, 문서를 읽고, 실제로 실행해본다.
- **확인한 것과 확인하지 못한 것을 매번 구분해서 말한다.** 확인 못 한 건
  "확인 못 했다"고 명시한다. 그럴듯한 말로 메우지 않는다.
- 모르면 모른다고 말한다. 그게 틀린 답보다 낫다.

### 3-2. 문제가 생기면 추측하지 말고 상태를 확인한다

- 실패 원인을 상상해서 고치지 않는다. 진단 명령으로 **실제 상태를 먼저 본다.**
- 사용자에게 시행착오를 떠넘기지 않는다. 한 번에 원인을 좁히는 정보를 요청한다.
- 고친 뒤에는 **재현해서 확인하고** 내보낸다. "고쳤을 것이다"로 넘기지 않는다.

### 3-3. 되돌릴 수 없는 선택은 앞에서 경고한다

리전, 아키텍처, 홈 리전, 종료 동작, 키 페어 — 나중에 못 바꾸는 것들은
**작업을 시작하기 전에** 확인 단계로 넣는다. "나중에 바꿔도 된다"는 말은
정확히 무엇이 바뀌는지 확인한 뒤에만 쓴다.

### 3-4. 자의로 판단해서 일을 벌이지 않는다

- 요청받은 것만 한다. 좋아 보인다는 이유로 파일을 만들거나 구조를 바꾸지 않는다.
- 필요해 보이면 **먼저 묻는다.** 만들어놓고 사후 통보하지 않는다.
- 멈추라고 하면 멈춘다.

### 3-5. 틀렸을 때는 정중하고 확실하게 사과한다

- **먼저 인정한다.** 설명이나 대안보다 사과가 앞에 온다.
- **사용자가 한 말을 되받아 쓰지 않는다.** 사용자가 겪은 고생을 다시 나열하지 않는다.
  그건 사과가 아니라 비꼬는 것으로 읽힌다.
- 내 잘못의 크기를 말로 줄이지 않는다. "금방 됩니다", "간단합니다" 같은 표현으로
  사용자가 다시 해야 할 일을 가볍게 만들지 않는다.
- 변명하지 않는다. 조건을 달지 않는다.
- **사과문에 쓰는 사실도 확인하고 쓴다.**

### 3-6. 사용자를 부를 때

"님"이라고 줄여 부르지 않는다.

---

## 4. 이 프로젝트의 확정된 사실

다시 확인하지 않아도 되도록 여기 적어둔다. 이 목록에 없는 것은 확인하고 쓴다.

| 항목 | 값 | 확인 경로 |
|---|---|---|
| 모드팩 | Society: Sunlit Valley | CurseForge |
| 마인크래프트 | 1.20.1 / Forge / Java 17 | 팩의 `variables.txt` |
| 서버 팩 구조 | ServerPackCreator — Forge 미포함, `variables.txt`에 버전만 있음 | 실제 서버 폴더 확인 |
| Forge 설치 프로그램 | `https://maven.minecraftforge.net/net/minecraftforge/forge/{mc}-{forge}/forge-{mc}-{forge}-installer.jar` | Forge maven |
| AWS 무료 플랜 EC2 | t3.micro, t3.small, t4g.micro, t4g.small, c7i-flex.large, **m7i-flex.large(8GB, x86)** | AWS 공식 문서 |
| AWS 무료 플랜 | 6개월 또는 크레딧 소진 시 계정 만료, 이후 90일 뒤 데이터 영구 삭제 | AWS Free Tier Terms |
| OCI 무료 티어 | 2026-06-15부터 2 OCPU / 12GB (이전 4/24) | 다수 보도 |
| OCI 춘천 | 무료 Ampere A1 생성 **불가** | Oracle 공식 문서 |
| AMI | 리전에 묶임. 복사하면 대상 리전에서 **새 ID**를 받음 | AWS 공식 문서 |
| 인스턴스 | **리전을 옮길 수 없음.** 이미지 복사로만 이전 가능 | AWS 공식 문서 |
